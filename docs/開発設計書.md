# 高齢者施設向け 食事管理Webアプリ 開発設計書

**プロジェクト名**: meal-management-app  
**作成日**: 2026年3月  
**目的**: ポートフォリオ用フルスタックWebアプリ開発

---

## 1. プロジェクト概要

高齢者施設の調理スタッフ向けに、食事提供業務を効率化するWebアプリケーション。
献立作成・材料計算・在庫管理・原価管理を一体化し、Claude APIによるAI機能を搭載する。

---

## 2. 技術スタック

| カテゴリ | 技術 | 備考 |
|---------|------|------|
| フロントエンド | Next.js 14 (App Router) + TypeScript | |
| UI | Tailwind CSS + shadcn/ui | |
| バックエンド | Next.js API Routes | |
| データベース | Supabase (PostgreSQL) | 認証・リアルタイム込み |
| AI | Anthropic Claude API | 献立自動生成 |
| スクレイピング | Cheerio + axios | レシピURL解析 |
| 通知 | LINE Messaging API | スタッフ通知 |
| デプロイ | Vercel | |
| 費用 | ほぼ0円 | 無料枠を活用 |

---

## 3. 機能要件（全11項目）

| No. | 機能 | 概要 | Phase |
|-----|------|------|-------|
| ① | 材料自動計算 | 人数入力で材料量を自動計算 | 1 |
| ② | 週間献立作成 | 1週間分の献立を作成・管理 | 1 |
| ③ | 原価計算 | レシート入力で材料単価・原価を計算 | 3 |
| ④ | 在庫管理 | 毎日の使用量と仕入れから在庫を把握 | 2 |
| ⑤ | レシピスクレイピング | URLから材料データを自動取得 | 2 |
| ⑥ | AI献立自動生成 | 材料データを元に1週間献立を自動作成 | 2 |
| ⑦ | 余り食材活用 | 余った材料から献立を再作成（10種類） | 2 |
| ⑧ | スタッフ作業管理 | シフト別タスク管理・LINE通知 | 3 |
| ⑨ | 栄養管理 | 1人前の栄養素表示・栄養士登録 | 3 |
| ⑩ | 月次統計 | 売上・食材ロス・原価の月次集計 | 3 |
| ⑪ | 販売管理 | フリーズドライ・チルド販売管理 | 3 |

---

## 4. DB設計

### テーブル一覧

```sql
-- 食材マスタ
CREATE TABLE ingredients (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  unit TEXT NOT NULL,
  unit_price DECIMAL(10,2),
  category TEXT,
  nutrition_per_100g JSONB,     -- {calories, protein, fat, carbs}
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- レシピ
CREATE TABLE recipes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  description TEXT,
  servings INT NOT NULL DEFAULT 1,
  meal_type TEXT,               -- 朝食, 昼食, 夕食
  source_url TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- レシピ材料
CREATE TABLE recipe_ingredients (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  recipe_id UUID REFERENCES recipes(id) ON DELETE CASCADE,
  ingredient_id UUID REFERENCES ingredients(id),
  amount DECIMAL(10,2) NOT NULL,
  unit TEXT NOT NULL
);

-- 週間献立
CREATE TABLE weekly_menus (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  week_start DATE NOT NULL,
  headcount INT NOT NULL,
  status TEXT DEFAULT 'draft',  -- draft, confirmed
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 献立詳細
CREATE TABLE menu_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  weekly_menu_id UUID REFERENCES weekly_menus(id) ON DELETE CASCADE,
  date DATE NOT NULL,
  meal_type TEXT NOT NULL,
  recipe_id UUID REFERENCES recipes(id),
  adjusted_servings INT
);

-- 在庫
CREATE TABLE inventory (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ingredient_id UUID REFERENCES ingredients(id),
  quantity DECIMAL(10,2) NOT NULL DEFAULT 0,
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 仕入れ記録
CREATE TABLE purchases (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ingredient_id UUID REFERENCES ingredients(id),
  quantity DECIMAL(10,2) NOT NULL,
  unit_price DECIMAL(10,2) NOT NULL,
  purchased_at DATE NOT NULL,
  receipt_image_url TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 在庫使用ログ
CREATE TABLE inventory_usage (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ingredient_id UUID REFERENCES ingredients(id),
  menu_item_id UUID REFERENCES menu_items(id),
  quantity_used DECIMAL(10,2) NOT NULL,
  used_at DATE NOT NULL
);

-- スタッフ
CREATE TABLE staff (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  role TEXT,
  line_user_id TEXT
);

-- シフト別タスク
CREATE TABLE shift_tasks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  date DATE NOT NULL,
  shift TEXT NOT NULL,          -- 朝, 昼, 夕
  staff_id UUID REFERENCES staff(id),
  task TEXT NOT NULL,
  is_done BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 5. ディレクトリ構成

```
meal-management-app/
├── app/
│   ├── (auth)/
│   │   ├── login/page.tsx
│   │   └── layout.tsx
│   ├── (dashboard)/
│   │   ├── layout.tsx
│   │   ├── page.tsx                    # ダッシュボード
│   │   ├── recipes/
│   │   │   ├── page.tsx                # レシピ一覧
│   │   │   ├── [id]/page.tsx           # レシピ詳細
│   │   │   └── new/page.tsx            # レシピ登録
│   │   ├── menu/
│   │   │   ├── page.tsx                # 週間献立一覧
│   │   │   ├── [id]/page.tsx           # 献立詳細
│   │   │   └── generate/page.tsx       # AI献立生成
│   │   ├── inventory/page.tsx
│   │   ├── cost/page.tsx
│   │   ├── nutrition/page.tsx
│   │   └── staff/page.tsx
│   └── api/
│       ├── recipes/route.ts
│       ├── menu/route.ts
│       ├── menu/generate/route.ts      # Claude API
│       ├── inventory/route.ts
│       ├── cost/route.ts
│       ├── scrape/route.ts             # スクレイピング
│       └── staff/route.ts
├── components/
│   ├── ui/                             # shadcn/ui
│   ├── recipes/
│   ├── menu/
│   ├── inventory/
│   └── shared/
│       ├── Navbar.tsx
│       ├── Sidebar.tsx
│       └── DataTable.tsx
├── lib/
│   ├── supabase/
│   │   ├── client.ts
│   │   └── server.ts
│   ├── claude.ts
│   ├── scraper.ts
│   └── utils.ts
├── types/index.ts
└── supabase/migrations/
    └── 001_initial_schema.sql
```

---

## 6. Claude API 活用設計

```typescript
// lib/claude.ts - 献立自動生成
const prompt = `
以下の条件で1週間の献立を作成してください：
- 使用可能食材: ${availableIngredients}
- 人数: ${headcount}名
- 予算上限: ${budget}円/日
- 除外献立（直近4週間）: ${recentMenus}
- 栄養バランスを考慮すること

JSON形式で返してください：
{ "monday": { "breakfast": "...", "lunch": "...", "dinner": "..." }, ... }
`;
```

---

## 7. 環境変数

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
LINE_CHANNEL_ACCESS_TOKEN=
```

---

## 8. 開発フロー

### Step 1: 環境セットアップ（Cursorのターミナルで実行）

```bash
npx create-next-app@latest meal-management-app \
  --typescript --tailwind --eslint --app \
  --src-dir=false --import-alias="@/*"
cd meal-management-app

npm install @supabase/supabase-js @supabase/ssr
npm install @anthropic-ai/sdk
npm install cheerio axios
npm install lucide-react class-variance-authority clsx tailwind-merge
npx shadcn@latest init
```

### Step 2: Supabaseセットアップ（ブラウザ）

1. https://supabase.com → New Project 作成
2. URL・anon key をコピー
3. `.env.local` に貼り付け
4. SQL Editorで `001_initial_schema.sql` を実行

### Step 3: Cursorで実装開始

この設計書をCursorのチャットに貼り付けて以下を指示：

```
この設計書に従って実装してください。
まず lib/supabase/client.ts と lib/supabase/server.ts を作成してください。
```

---

## 9. フェーズ別スケジュール

| Phase | 期間 | 内容 |
|-------|------|------|
| Phase 1 | Week 1-2 | 食材マスタ・レシピCRUD・週間献立・人数計算 |
| Phase 2 | Week 3-4 | スクレイピング・AI献立生成・在庫管理 |
| Phase 3 | Week 5-6 | 原価統計・栄養管理・スタッフ管理・LINE通知 |
| 仕上げ | Week 7 | README完成・スクリーンショット・デプロイ |

---

## 10. 納品物（ポートフォリオとして）

- GitHubリポジトリ（ソースコード）
- Vercelデプロイ済みデモURL
- README.md（セットアップ手順・機能説明・スクリーンショット）
